spring:
  application:
    name: cms-form-extractor
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

server:
  port: 8080

ollama:
  base-url: http://localhost:11434
  model: qwen2.5vl:7b
  timeout-seconds: 0

form:
  extraction:
    max-retries: 5
    confidence-threshold: 0.7
